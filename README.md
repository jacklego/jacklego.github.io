# jacklego.github.io (AKA Peter's Pumpkin Patch)

Homepage message: edit in (_homepage-memo.md)[_homepage-memo.md]. 

To edit a year's image thumbnail: 
1. Go to the relevant year's `index.qmd` file
2. Edit the line that starts with `image: ` (should be near the bottom)
3. Change it to another thumbnail 
  - Make sure to match the formatting for the thumbnail: include the path and target the `__thumb` file

To build: go to `Actions` tab in Github. 

